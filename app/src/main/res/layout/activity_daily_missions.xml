<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent" android:orientation="vertical"
    android:padding="24dp" android:background="@color/background">
    <TextView android:layout_width="match_parent" android:layout_height="wrap_content"
        android:text="@string/daily_missions_title" android:textSize="28sp" android:textStyle="bold" android:textColor="@color/primary" />
    <TextView android:id="@+id/mission_win_match" style="@style/MissionRow" />
    <TextView android:id="@+id/mission_send_chat" style="@style/MissionRow" />
    <TextView android:id="@+id/mission_play_friendly" style="@style/MissionRow" />
    <TextView android:id="@+id/mission_win_tournament" style="@style/MissionRow" />
    <TextView android:id="@+id/text_all_missions_reward" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:layout_marginTop="24dp" android:textSize="16sp" />
</LinearLayout>
